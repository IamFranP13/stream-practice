package exercises.medium;

import java.util.List;
import java.util.Map;
import java.util.stream.Collectors;

import static java.util.stream.Collectors.groupingBy;

/**
 * Count frequency of each number in a list
 * Medium difficulty: requires groupingBy with counting collector
 */
public class CountFrequency {

    /**
     * Counts the frequency of each number in the list
     * 
     * @param numbers list of integers
     * @return map where key is the number and value is its frequency
     */
    public static Map<Integer, Long> countFrequency(List<Integer> numbers) {
        // TODO: Implementa este método usando streams
        // Pista: usa Collectors.groupingBy() con Collectors.counting()
        // SOLUTION START
        throw new UnsupportedOperationException("Implementa este método");
        // SOLUTION END
    }
}
